# Персональный финансовый советник

Telegram бот для учета доходов и расходов с интеграцией LLM через OpenRouter или Ollama.

## Возможности

- ✅ Извлечение транзакций из текстовых сообщений
- ✅ Обработка изображений чеков и скриншотов
- ✅ Обработка голосовых сообщений (транскрипция через Whisper)
- ✅ Автоматическая категоризация транзакций
- ✅ Отчеты о балансе и статистике
- ✅ История всех транзакций
- ✅ Поддержка локальных моделей через Ollama

## Установка

### Требования

- Python 3.11+
- [uv](https://github.com/astral-sh/uv) - менеджер зависимостей
- [ffmpeg](https://ffmpeg.org/) - для обработки аудио файлов (требуется для Whisper)

### Шаги установки

1. Клонируйте репозиторий:
   ```bash
   git clone <repository-url>
   cd telegram-llm-bot
   ```

2. Установите зависимости:
   ```bash
   make install
   ```

3. Установите ffmpeg (требуется для обработки голосовых сообщений):
   ```bash
   # macOS
   brew install ffmpeg
   
   # Ubuntu/Debian
   sudo apt-get update && sudo apt-get install ffmpeg
   
   # Windows (через Chocolatey)
   choco install ffmpeg
   ```

## Конфигурация

### 1. Получите токен Telegram бота

1. Найдите @BotFather в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

### 2. Выберите провайдера LLM

#### Вариант A: OpenRouter (облачный)

1. Зарегистрируйтесь на [OpenRouter.ai](https://openrouter.ai/)
2. Перейдите в раздел API Keys
3. Создайте новый ключ

#### Вариант B: Ollama (локальная модель)

1. Установите Ollama:
   ```bash
   # macOS / Linux
   curl -fsSL https://ollama.ai/install.sh | sh
   
   # или через Homebrew на macOS
   brew install ollama
   ```

2. Запустите Ollama сервер:
   ```bash
   ollama serve
   ```

3. Установите модель (в отдельном терминале):
   ```bash
   # Для текстовых сообщений (с поддержкой structured output)
   ollama pull llama3.2
   
   # Для изображений (опционально, для итерации 5)
   ollama pull llama3.2-vision
   ```

### 3. Настройте переменные окружения

1. Скопируйте пример конфигурации:
   ```bash
   cp .env.example .env
   ```

2. Отредактируйте `.env` файл:

   **Для OpenRouter:**
   ```bash
   TELEGRAM_TOKEN=ваш_токен_от_BotFather
   OPENAI_API_KEY=ваш_ключ_от_OpenRouter
   OPENAI_BASE_URL=https://openrouter.ai/api/v1
   MODEL_TEXT=openai/gpt-oss-20b:free
   MODEL_IMAGE=meta-llama/llama-3.2-11b-vision-instruct
   ```

   **Для Ollama:**
   ```bash
   TELEGRAM_TOKEN=ваш_токен_от_BotFather
   OPENAI_API_KEY=ollama
   OPENAI_BASE_URL=http://localhost:11434/v1
   MODEL_TEXT=llama3.2
   MODEL_IMAGE=llama3.2-vision
   ```

**Описание переменных:**

- `TELEGRAM_TOKEN` - токен бота от @BotFather (обязательно)
- `OPENAI_API_KEY` - API ключ от OpenRouter или любое значение для Ollama (обязательно)
- `OPENAI_BASE_URL` - URL API провайдера (по умолчанию: https://openrouter.ai/api/v1)
- `MODEL_TEXT` - модель для обработки текстовых сообщений
- `MODEL_IMAGE` - модель для обработки изображений (должна поддерживать vision)

## Запуск

```bash
make run
```

Бот запустится в режиме polling и будет работать до остановки (Ctrl+C).

**Примечание:** При первом запуске модель Whisper "base" будет автоматически загружена из интернета (размер ~140MB). Последующие запуски будут использовать уже загруженную модель.

## Использование

### Команды бота

- `/start` - начать новый диалог (сбрасывает историю и транзакции)
- `/balance` - показать баланс, доходы, расходы и статистику по категориям
- `/transactions` - показать список всех транзакций

### Примеры использования

**Пример 1: Текстовая транзакция**
```
Пользователь: Сегодня купил продукты на 1500 рублей
Бот: Записал ваш расход на продукты в размере 1500 рублей.

✅ Найдено и сохранено 1 транзакция
💵 Баланс: -1500 руб.
```

**Пример 2: Обработка чека**
```
Пользователь: [отправляет фото чека]
Бот: Обработал чек. Найдено 3 позиции на сумму 2340 рублей.

✅ Найдено и сохранено 1 транзакция
💵 Баланс: -3840 руб.
```

**Пример 3: Обработка голосового сообщения**
```
Пользователь: [отправляет голосовое сообщение: "Сегодня купил кофе на 300 рублей"]
Бот: ⏳ Вы сказали: Сегодня купил кофе на 300 рублей
     Записал ваш расход на кофе в размере 300 рублей.

✅ Найдено и сохранено 1 транзакция
💵 Баланс: -4140 руб.
```

**Пример 4: Проверка баланса**
```
Пользователь: /balance
Бот: 💵 Отчет о балансе

📊 Баланс: -1500.00 руб.
💰 Доходы: 0.00 руб.
💸 Расходы: 1500.00 руб.

📈 Всего транзакций: 1

**Статистика по категориям:**
💸 продукты: -1500.00 руб.
```

## Обработка голосовых сообщений (Whisper)

Бот поддерживает обработку голосовых сообщений через OpenAI Whisper:

- **Модель**: Whisper "base" (загружается автоматически при первом запуске)
- **Язык**: Русский (можно изменить в коде)
- **Процесс**: Голосовое сообщение → транскрипция через Whisper → извлечение транзакций через LLM
- **Требования**: Установленный ffmpeg для обработки аудио файлов

### Установка ffmpeg

**macOS:**
```bash
brew install ffmpeg
```

**Ubuntu/Debian:**
```bash
sudo apt-get update && sudo apt-get install ffmpeg
```

**Windows:**
```bash
# Через Chocolatey
choco install ffmpeg

# Или скачайте с https://ffmpeg.org/download.html
```

### Использование

Просто отправьте голосовое сообщение в бот с описанием транзакции. Бот автоматически:
1. Скачает голосовой файл
2. Транскрибирует его через Whisper
3. Извлечет транзакции из транскрибированного текста
4. Сохранит транзакции и покажет результат

## Ограничения

- Максимальная длина текстового сообщения: 4000 символов
- История диалога и транзакции хранятся в памяти (при перезапуске бота теряются)
- Бот обрабатывает текст, изображения и голосовые сообщения (не PDF, не файлы других форматов)
- Для обработки изображений требуется модель с поддержкой vision
- Для обработки голосовых сообщений требуется установленный ffmpeg
- Модель Whisper загружается в память при старте бота (модель "base" ~140MB)
- Первая загрузка модели Whisper может занять некоторое время

## Разработка

### Структура проекта

```
├── src/
│   ├── bot.py          # Точка входа, инициализация (загрузка модели Whisper)
│   ├── config.py       # Загрузка конфигурации
│   ├── handlers.py     # Обработчики сообщений и команд (текст, изображения, голос)
│   ├── llm.py          # Интеграция с LLM и Whisper
│   └── models.py       # Pydantic модели для транзакций
├── prompts/
│   ├── system_prompt_text.txt   # Системный промпт для текстовых сообщений
│   └── system_prompt_image.txt  # Системный промпт для изображений
├── .env                # Конфигурация (не коммитится)
├── .env.example        # Пример конфигурации
├── Makefile            # Команды для работы
├── pyproject.toml      # Зависимости проекта
└── README.md           # Документация
```

### Команды Makefile

- `make install` - установить зависимости
- `make run` - запустить бота

## Лицензия

MIT

